[project]
name = "fastapi-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ephem-debugger",
    "fastapi[standard]>=0.135.1",
]

[tool.uv.sources]
ephem-debugger = { path = "../../../packages/debugger-py", editable = true }
