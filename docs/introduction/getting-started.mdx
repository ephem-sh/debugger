---
title: Getting started
description: Install and set up debugger in your project.
order: 2
---

# Getting started

Get debugger running in your project in under two minutes. This guide
covers installation and basic usage. For framework-specific setup, see
the guides linked below.

## Install

Add `@ephem-sh/debugger` as a dev dependency:

```bash
# npm
npm install -D @ephem-sh/debugger

# pnpm
pnpm add -D @ephem-sh/debugger

# bun
bun add -D @ephem-sh/debugger

# yarn
yarn add -D @ephem-sh/debugger
```

## Framework setup

debugger uses framework-specific adapters to wire into your dev server.
Each adapter handles server instrumentation and browser script injection.

| Framework | Adapter | Guide |
|-----------|---------|-------|
| Next.js (App Router) | `@ephem-sh/debugger/nextjs` | [Guide](/examples/nextjs) |
| React + Vite | `@ephem-sh/debugger/vite` | [Guide](/examples/vite-react) |
| Vue + Vite | `@ephem-sh/debugger/vite` | [Guide](/examples/vite-vue) |
| SvelteKit | `@ephem-sh/debugger/vite` + `vite/sveltekit` | [Guide](/examples/sveltekit) |
| React Router v7 | `@ephem-sh/debugger/vite` + `vite/react-router` | [Guide](/examples/react-router) |
| Astro | `@ephem-sh/debugger/vite` + `vite/astro` | [Guide](/examples/astro) |
| Nuxt | `@ephem-sh/debugger/vite` + `nitro` | [Guide](/examples/nuxt) |
| Qwik | `@ephem-sh/debugger/vite` | [Guide](/examples/qwik) |
| TanStack Start | `@ephem-sh/debugger/vite` + `vite/tanstack-start` | [Guide](/examples/tanstack-start) |
| Waku | `@ephem-sh/debugger/vite` | [Guide](/examples/waku) |
| Angular | `@ephem-sh/debugger/angular` | [Guide](/examples/angular) |

## Use the CLI

Once your dev server is running with debugger instrumented, query it
with `dbg`:

```bash
# Check session status
npx dbg status

# View browser console logs
npx dbg browser console

# View server console logs
npx dbg server console

# View everything
npx dbg all

# JSON output for agents
npx dbg browser console --json
```

With bun:

```bash
bunx dbg status
bunx dbg browser console
```

## CLI overview

The CLI uses a two-level command structure split by where the data
comes from:

```
dbg <scope> <command> [flags]
dbg <command> [flags]
```

**Scopes:**
- `browser` — browser-side data (console, network, errors, cookies,
  storage, workers, cache, permissions, quota)
- `server` — server-side data (console, errors)

**Top-level commands:**
- `status` — session info
- `all` — everything merged (default)

Run `npx dbg --help` for the full command and flag reference.

## Next steps

- [CLI reference](/api/reference) — all commands, flags, and examples
- Framework guides: [Next.js](/examples/nextjs),
  [React + Vite](/examples/vite-react),
  [Vue](/examples/vite-vue),
  [SvelteKit](/examples/sveltekit),
  [React Router](/examples/react-router),
  [Astro](/examples/astro),
  [Nuxt](/examples/nuxt),
  [Qwik](/examples/qwik),
  [TanStack Start](/examples/tanstack-start),
  [Waku](/examples/waku),
  [Angular](/examples/angular)
